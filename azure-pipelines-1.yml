# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest


  stages:
  - stage: Build Stage
    jobs:
    - job: job1
      steps:
        bash: echo "Hello this is build stage"
    
  - stage: Test Stage
    jobs:
    - job:  job2
      steps: 
       bash : echo "this is Test Stage"
  - stage:
    jobs:
    - job: QADeploy
      steps:
        bash: this is QA Deploy
  - stage:
    jobs: 
    - job: ProdDeploy
      steps:
        bash: this is Prod Deploy